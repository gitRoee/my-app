import { router } from "expo-router";
import { View, Text, Pressable } from "react-native"

const index = () => {

    return (
        <View>
            {   
                // <Pressable onPress={() => router.push("/pages/1")}>
                //<Text>Home</Text>
                //</Pressable>
            }
        </View >
    )
};

export default index;